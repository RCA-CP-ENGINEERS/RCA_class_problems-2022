#include<bits/stdc++.h>
using namespace std;


void print(string last, string first){
    cout << " Name in reverse  is : " << last  << " " << first <<endl;
}
int main(){

    string first, last;
    cin >> first >> last;
    print(last, first);

}