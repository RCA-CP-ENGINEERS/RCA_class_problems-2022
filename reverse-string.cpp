#include<bits/stdc++.h>
using namespace std;


void reverse(string text){
    string reversed;
    int i;
    for(i = text.size(); i > 0; i--){
       cout << text[i];
    }

   
}
int main(){

    string input;
    cin >> input;

    cout << "revesed " <<endl;
    reverse(input);

    return 0;
}