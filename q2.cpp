#include<iostream>
using namespace std;
int main()
{
    int val;
    string result;
    string grade; 
    int a = 12; int  b = 25;

    // cout<< "Enter  marks : ";
    // cin>>val;
    // result =  val >= 20 ? "passed " : "failed";
    // grade = val >= 16 ? "A" :  val > 14 ?  " D "  : val > 10 ? " C" : "F";

    //bitwise and operator  oprator will compare bits instead of comparing expressions like logical and operator
    // cout << " (a & b ) equals to:  " << ( a & b) <<endl;
    // cout << " (a  | b) equals to : " << ( a | b ) <<endl;
    // // cout << " ( a OR b ) equals to :  " << ( a XOR b) << endl;
    // cout << " ( !a ) equals to : " << ( !a ) << endl;
    // //bitwice left shift operator
    // cout << " ( a << 21) equals to :" << ( a << 1) << endl;
    // cout << " ( a << 2) equals to :" << ( a << 2) << endl;
    // cout << " ( a << 3) equals to :" << ( a << 3) << endl;


    // cout << " ( a >> 1) equals to :" << ( a >> 1) << endl;
    // cout << " ( a >> 2) equals to :" << ( a >> 2) << endl;
    // cout << " ( a >> 3) equals to :" << ( a >> 3) << endl;


    cout << '10' - '0';

    // cout << result <<endl;
    // cout << "Your in  grade " << grade <<endl;
    return 0;
}