#include<bits/stdc++.h>
using namespace std;
typedef vector<int> vi;
int getDiffrence(int number[]){
    cout << sizeof(numer);
    cout 
    return sizeof(number) / sizeof(number[0]);
}

int main(){
    int numbers[4] = {1,2,3,6};
    cout <<  getDiffrence(numbers);

    return 0;
}